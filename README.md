# TechWeirdo-Task

// Install nodemodules before run the project using : npm install 
// After Installing node modules run the project using : ng serve -o 
// our projectc will open directly in the browser after the compilation
